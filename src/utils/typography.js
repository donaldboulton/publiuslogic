const typography = {
  fonts: `kaushan-script, BlinkMacSystemFont, "Segoe UI", "Helvetica Neue", Arial, sans-serif`,
  // font sizes and line heigths in em units
  minFontSize: 1,
  maxFontSize: 1.2,
  minLineHeight: 1.5,
  maxLineHeight: 1.8,
}

export default typography
